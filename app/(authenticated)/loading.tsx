import { RouteLoading } from "@/components/domain/route-loading";

/** Keeps the application shell responsive while a destination module streams. */
export default function AuthenticatedLoading() {
  return <RouteLoading />;
}
