# Database

This folder is reserved for the standalone application's owned schema, migrations, seeds, and database-specific documentation.

Do not add generated types, migrations, connection details, or references from the source repository. The schema work starts with the workspace/access and CRM data model described in `../docs/target-architecture.md` and must resolve the blockers in `../docs/backend-gap-register.md`.
